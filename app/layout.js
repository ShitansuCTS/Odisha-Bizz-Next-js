import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "react-hot-toast";
import Navbar from "@/components/navbar/Navbar";
import Footer from "@/components/footer/Footer";
import AuthProvider from "@/components/AuthProvider";
import ScrollToTopButton from "@/components/scroller/ScrollToTopButton";
import Script from "next/script";


const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Welcome to Odisha Bizz",
  description: "Generated by create next app",
  icons: {
    icon: "/logo-fav.ico",          // fallback favicon
    shortcut: "/favicon-32x32.png", // optional
    apple: "/apple-touch-icon.png", // for iOS
  },
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">

      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Navbar />
        <AuthProvider>{children}</AuthProvider>
        <Toaster position="top-center" />
        <Footer />
        <ScrollToTopButton />
        <Script chatbot_id="6911bdc4b446537655579714" data-type="default" src="https://app.thinkstack.ai/bot/thinkstackai-loader.min.js"></Script>
      </body>
    </html>
  );
}
